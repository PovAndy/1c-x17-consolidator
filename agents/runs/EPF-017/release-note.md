# Release Note

Process hardening for QA:

- BSL/query/string edits now require explicit runtime-verification status;
- without runtime 1C verification, such tasks can only reach `accepted_with_ops_check`, not full `accepted`.
