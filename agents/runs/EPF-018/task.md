# EPF-018

## Goal
Introduce isolated work packets for `engineer` and `qa` so coding and verification run on task-local context instead of whole-project context.

## Scope
- add isolation protocol;
- add `coder-packet.md` and `tester-packet.md` templates;
- update runbook, self-contract, message schema, coder and tester roles.
