# EPF-013

## Goal
Introduce a lightweight semantic CLI for `epf1129` to reduce token usage and improve reliable navigation across `Form.xml`, form modules and `ObjectModule.bsl`.

## Scope
- add local CLI script;
- cover project overview, forms, commands, events, defs, symbol search;
- wire it into runbook, setup and VSCode tasks.

## Out of Scope
- full MCP server;
- replacement for compile/runtime validation;
- deep semantic analysis beyond project-local structure.
