# Impact Map

- Added `scripts/semantic_cli.py` as a local semantic navigation tool.
- Added usage guide in `docs/semantic-cli.md`.
- Updated `AGENT_OPERATIONS.md` and `PROFESSIONAL_SETUP.md` to prefer semantic navigation before raw `rg` for 1C form/module work.
- Added VSCode tasks for overview, main-form commands and main-form events.

## Risk
- Low.
- No runtime change in EPF logic.
- Changes affect only local tooling and workflow.
