# Release Note

Added a lightweight semantic CLI for the `epf1129` project.

New capability:
- compact semantic overview of the EPF project;
- form command/event navigation without repeated raw text search;
- direct symbol lookup across `BSL/XML`.

Operational effect:
- lower token usage for agent work;
- faster context bootstrap on large 1C forms;
- more reliable entry-point discovery before edits.
