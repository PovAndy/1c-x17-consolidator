# Release Note

`v25-113.8`

Fixed the database structure analysis so that:
- organizations quick-check uses valid query syntax;
- readiness verdict no longer reports false `PASS/OK` if organizations analysis fails.
