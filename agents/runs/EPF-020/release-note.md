# Release Note

Added a dedicated read-only diagnostics mode for lost links between personal accounts, services and `Вид объекта учёта` after XML import. The feature is intended to produce factual evidence before any recovery or loader fix is implemented.
