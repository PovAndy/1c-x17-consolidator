[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "chromadb"
version = "0.0.0.dev0"
description = "Local ChromaDB compatibility backend for MemPalace pilot in epf1129"
requires-python = ">=3.9"

[tool.setuptools]
packages = ["chromadb"]
